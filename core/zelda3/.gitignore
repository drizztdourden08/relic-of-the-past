# A ROM, a save or an extracted asset blob must never be committed — this repo is
# public. Everything else upstream ignored here belonged to build paths this copy
# no longer has.
/zelda3.sfc
/zelda3.smc
/zelda3_assets.dat
/saves/
/msu/
