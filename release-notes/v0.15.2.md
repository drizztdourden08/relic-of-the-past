<!-- @layer root-config @kind doc -->
# Relic of the Past v0.15.2

Finishes the Linux half of the controller fix from 0.15.1.

## Controllers

- Controllers that need libusb now work on Linux. The 0.15.1 Linux build was made without libusb support, so that fix only reached macOS.
