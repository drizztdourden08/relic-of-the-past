<!-- @layer root-config @kind doc -->
# Relic of the Past v0.20.7

The Dialog Box settings are reachable again.

## Fixes

- Every row under HUD, Dialog Box greyed out while Pattern was set to None, which is its default. The size, font, border and background rows are live again as soon as Box is set to Enhanced. Only the pattern rows follow the Pattern setting, as they always should have.
