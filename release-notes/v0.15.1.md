<!-- @layer root-config @kind doc -->
# Relic of the Past v0.15.1

A controller fix for Linux and macOS.

## Controllers

- Controllers that need libusb now work on Linux and macOS out of the box. The library ships with the app, so nothing has to be installed first. Windows already worked this way.
