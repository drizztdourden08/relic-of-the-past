<!-- @layer root-config @kind doc -->
# Relic of the Past v0.16.1

A test build for checking the update and install path. Not a real release, and it will be removed.

## What is in it

- A large red TEST across the app window, so an update can be confirmed at a glance.
- The same marker in the installer, which also reports itself as installer 2.
- Nothing else changed.

## If you installed this by accident

Update again once this release is gone, or reinstall from the release marked latest.
