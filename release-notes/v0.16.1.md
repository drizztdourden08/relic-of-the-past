<!-- @layer root-config @kind doc -->
# Relic of the Past v0.16.1

A version number fix in the installer.

## Installer

- The setup window reported itself as installer 2.0 when it has only ever been the first one. It now says 1.0, matching what the file's own properties have always claimed.
