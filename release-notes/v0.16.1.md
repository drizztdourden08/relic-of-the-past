<!-- @layer root-config @kind doc -->
# Relic of the Past v0.16.1

A test build for checking the update path. Not a real release, and it will be removed.

## Starting up

- The wait before the app appears now has its own small window, and the main window only appears once it is actually ready. It used to open small, resize itself, and assemble the interface in front of you.

## Still in from the last test build

- A large red TEST across the app window, so an update can be confirmed at a glance.
- The same marker in the installer, which also reports itself as installer 2.

## If you installed this by accident

Update again once this release is gone, or reinstall from the release marked latest.
