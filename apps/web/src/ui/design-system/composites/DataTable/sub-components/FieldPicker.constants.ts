/* @layer renderer-components @kind constants */

/** Mirrors the panel's CSS min-width. Used to flip a submenu that would overflow the viewport before it exists to be measured. */
const SUB_PANEL_WIDTH = 180;

export { SUB_PANEL_WIDTH };
