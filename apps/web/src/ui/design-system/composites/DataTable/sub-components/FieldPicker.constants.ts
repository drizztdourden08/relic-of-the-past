/* @layer renderer-components @kind constants */

/**
 * Mirrors the panel's CSS min-width, for a nested submenu opening to the side
 * of its parent. Used only to flip one that would overflow the viewport — the
 * panel has to be placed before it exists to be measured.
 */
const SUB_PANEL_WIDTH = 180;

export { SUB_PANEL_WIDTH };
