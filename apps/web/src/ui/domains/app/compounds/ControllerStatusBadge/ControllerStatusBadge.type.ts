/* @layer renderer-components @kind types */
import type { DeviceStatus } from '@shared/ipc';

interface ControllerStatusBadgeProps {
  status: DeviceStatus;
}

export type { ControllerStatusBadgeProps };
