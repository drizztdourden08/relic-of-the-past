/* @layer renderer-components @kind types */

interface PondStatusNoteProps {
  /** One plain sentence per point, already derived from the live settings. */
  lines: readonly string[];
}

export type { PondStatusNoteProps };
