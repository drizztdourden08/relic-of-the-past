/* @layer renderer-components @kind data */
/** Fixed copy for the progressive-tier block. */

const TIERS_TITLE = 'Tiered items';

const TIERS_CAPTION = 'Untick a rung to take it out of the seed.';

/** Shown above the note when the ticks have closed something the seed needs. */
const BLOCKED_LEAD = 'This set cannot be rolled:';

export { BLOCKED_LEAD, TIERS_CAPTION, TIERS_TITLE };
