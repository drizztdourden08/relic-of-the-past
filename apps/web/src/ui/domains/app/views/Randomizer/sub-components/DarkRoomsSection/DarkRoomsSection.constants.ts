/* @layer renderer-components @kind data */
/** Fixed copy for the dark-room section. The controls say the rest. */

const DARK_ROOMS_TITLE = 'Dark rooms';

const LIGHTS_LABEL = 'Lights';

/** The one thing the controls cannot show: the seed will be rolled blind. */
const BLIND_NOTE = 'No light ticked, so the seed may send you through a dark room blind.';

export { BLIND_NOTE, DARK_ROOMS_TITLE, LIGHTS_LABEL };
