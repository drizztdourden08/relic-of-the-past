/* @layer renderer-components @kind logic */
/** The two starting points the creation form offers before a player customizes anything. */
type ProfilePresetId = 'vanilla' | 'enhanced';

export type { ProfilePresetId };
