/* @layer renderer-hud @kind constants */
/** Gap between the bottom of the frame and the countdown disc, in HUD tiles: the HUD's side margin. */
const BOTTOM_MARGIN_TILES = 2;
/** Fade of the whole countdown as it comes and goes, in milliseconds. */
const COUNTDOWN_FADE_MS = 400;

export { BOTTOM_MARGIN_TILES, COUNTDOWN_FADE_MS };
