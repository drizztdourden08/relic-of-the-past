/* @layer renderer-hud @kind types */

interface TitleViewProps {
  /** The profile whose battery save decides the sword tier and the world; null shows the default. */
  profileId: string | null;
}

export type { TitleViewProps };
